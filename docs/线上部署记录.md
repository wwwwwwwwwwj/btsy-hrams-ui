# 线上部署记录

记录时间：2026-08-31  
环境：仅内网可访问

## 服务器

| 项 | 值 |
| --- | --- |
| 地址 | `10.45.41.2` |
| SSH | 端口 22，用户 `ubuntu`（密码认证） |
| 部署目录 | `/mnt/app` |
| 前端静态资源 | `/mnt/app/nginx/html` |
| 后端镜像构建目录 | `/mnt/app`（`Dockerfile` + `btsy-admin.jar`） |
| Compose | `/mnt/app/docker-compose.yml` |

## 运行中的容器

| 容器 | 镜像 | 说明 |
| --- | --- | --- |
| `btsy-hrams` | `btsy-hrams:1.0.0` | 后端，容器内 8080 |
| `nginx` | `nginx:latest` | 80/443，反代 `/prod-api/` → `btsy-hrams:8080` |
| `mysql` | `mysql:8.0.31` | 宿主机 `13306` → 3306 |
| `redis` | `redis:8.2` | 宿主机 `16379` → 6379 |
| `minio` | `quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z` | `19000`/`19001` |

## 本次发布内容

- 前端：仓库 `main`（`fc9801f`）+ 本地未提交改动（综合查询「材料件数」、批量挂接人数统计、目录树展开/收起与异常标红）
- 后端：仓库 `main`（`84b768c`）打的 `btsy-admin.jar`（本地无业务代码改动；未带入本机 `application-dev.yml` 的 OCR/Dify 地址）
- 本机打包默认会写入 `dev` 配置。已在 compose 增加 `SPRING_PROFILES_ACTIVE: prod`，否则会连 `127.0.0.1:6380` 启动失败

## 发布步骤（备忘）

1. 前端：`nvm use 20 && npm run build`，打包 `dist` 的 `index.html` / `favicon.ico` / `assets`
2. 后端：JDK 17，`mvn -pl btsy-admin -am -DskipTests package`，产物 `btsy-admin/target/btsy-admin.jar`
3. 上传覆盖 `/mnt/app/btsy-admin.jar`，替换 nginx html（保留原有 `json/`、`tinymce/`）
4. `cd /mnt/app && sudo docker build -t btsy-hrams:1.0.0 .`
5. `sudo docker compose up -d --no-deps --force-recreate btsy-hrams`

## 回滚

备份目录：`/mnt/app/backup/202608310131/`（旧 `btsy-admin.jar`、旧 `index.html`）

还原 jar 后重新 `docker build` 并 `force-recreate btsy-hrams`；前端把备份的 `index.html` 拷回，并视需要恢复当时的 `assets`。
