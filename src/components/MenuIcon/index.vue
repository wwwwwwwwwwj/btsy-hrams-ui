<template>
  <template v-if="icon">
    <img
      v-if="isSimpleTheme"
      :src="imgIconUrls[`/src/assets/menu-icons/${icon}.png`] || defaultImgUrl"
      :style="[{ width: '22px', height: '22px', background: 'none' }, imgStyle]"
      :class="imgClass"
    />
    <ElIcon
      v-else
      :style="componentStyle"
      :class="componentClass"
      v-bind="componentProps || {}"
    >
      <component :is="icon" :style="iconStyle" />
    </ElIcon>
  </template>
</template>

<script setup>
  import { imgIconUrls, useIsSimpleTheme } from '@/components/IconSelect/util';
  const defaultImgUrl =
    imgIconUrls['/src/assets/menu-icons/IconProLinkOutlined.png'];

  defineOptions({ name: 'MenuIcon' });

  defineProps({
    /** 图标名称 */
    icon: String,
    /** 图标组件属性(非清新主题) */
    componentProps: Object,
    /** 图标图片类名(非清新主题) */
    componentClass: String,
    /** 图标组件样式(非清新主题) */
    componentStyle: Object,
    /** 图标 svg 样式(非清新主题) */
    iconStyle: Object,
    /** 图标图片类名(清新主题) */
    imgClass: String,
    /** 图标图片样式(清新主题) */
    imgStyle: Object
  });

  /** 是否是清新主题 */
  const { isSimpleTheme } = useIsSimpleTheme();
</script>
